<p id="notice"><%= notice %></p>

<div class="jumbotron text-center">
      <%= image_tag @athlete.avatar.url(:medium), class: "clearfix" %>  
      <div class="clearfix"></div>
  <div class="col-md-6 clearfix">
    
      <h1><%= @athlete.name %></h1>
  </div>   
    <div class="col-md-6 clearfix">
    </div>
      <h1><%= @athlete.nationality %></h1>
</div>

<div class="col-md-6">
          <%= image_tag @athlete.runs.each.first.image.url(:thumb) %>
</div>
<div class="col-md-6">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Event</th>
        <th>Score</th>
      </tr>
    </thead>
    <tbody>
    <% @athlete.runs.each do |run| %>
      <tr class="success">
        <td>
        <%= link_to run.event.title, class: "btn btn-success"  %>
        </td>
        <td>
        <%= link_to run.score, class: "btn btn-success"  %></td>
  <% end %>

      <br/>       
      </tr>
    </tbody>
  </table>
</div>

<table class="table table-hover">
    <thead>
      <tr>
        <th>Event</th>
        <th>Score</th>
      </tr>
    </thead>
    <tbody>
    <% @athlete.runs.each do |run| %>
      <tr class="success">
        <td>
        <%= link_to run.event.title, class: "btn btn-success"  %>
        </td>
        <td>
        <%= link_to run.score, class: "btn btn-success"  %></td>
	<% end %>

	  	<br/>       
      </tr>
    </tbody>
  </table>
<%= link_to 'Edit', edit_athlete_path(@athlete) %> |
<%= link_to 'Back', athletes_path %> |
<%= link_to 'Destroy', @athlete, method: :delete, data: { confirm: 'Are you sure?' } %>
